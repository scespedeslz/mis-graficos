<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Análisis de Energía</title>
    <style>
        /* 1. ESTILOS (Lo que no se ve, pero da forma) */
        body { 
            margin: 0; 
            padding: 40px; 
            background-color: #f0f2f5; /* Gris clarito de fondo */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            justify-content: center;
        }
        #grafico-energia {
            max-width: 800px;
            width: 100%;
        }
        h2 { color: #2c3e50; margin-bottom: 10px; }
        p { color: #5d6d7e; margin-bottom: 25px; line-height: 1.5; }
        
        .contenedor-blanco {
            background: white; 
            padding: 30px; 
            border-radius: 15px; 
            box-shadow: 0 10px 25px rgba(0,0,0,0.1); 
            text-align: center;
        }
        svg { width: 100%; height: auto; }
    </style>
</head>

<body> <section id="grafico-energia">
        <h2>¿Cómo influyen mis hábitos diarios en la distribución de mi energía vital?</h2>
        <p>Para este prototipo, utilicé este gráfico para procesar un registro de una semana de datos personales. Clasifiqué las actividades en categorías (Salud, Alimentación, Productivo, Ocio y Descanso).</p>
        
        <div class="contenedor-blanco">
            <svg viewBox="0 0 800 400">
                </svg>
        </div>
    </section>

</body>
</html>
